{
  "engines": {
    "node": "^12.16.2",
    "yarn": "^1.22.4",
    "npm": "^6.14.4"
  },
  "scripts": {
    "build": "cd ./client && yarn install --production && yarn build",
    "dev": "FLASK_ENV=development pipenv run flask run",
    "upgrade": "FLASK_ENV=development pipenv run flask db upgrade",
    "install": "pipenv install --dev"
  }
}
